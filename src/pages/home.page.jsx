import React from "react";
import Auth from "../components/auth/auth";

const Home = () => {
  return (
    <main>
      <section>
        <Auth />
      </section>
    </main>
  );
};

export default Home;
